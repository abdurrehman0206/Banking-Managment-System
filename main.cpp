#include "functions.h"


int main(){
    // using main for only testing
    //main is hardcoded for now
    id_assign(accNumChk);
    //i_data(&person, &n);
    o_data(person, &n);
    system("pause");
    return 0;
}